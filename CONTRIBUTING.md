# 贡献指南

这是一个安全敏感的 Windows 本地插件。任何便利性改动都不能削弱写入门禁、凭据隔离和执行后验收。

## 开发环境

- Windows
- PowerShell 5.1 或更高版本
- Node.js 20 或更高版本
- Git

安装依赖并运行完整检查：

```powershell
Set-Location .\plugins\baidu-netdisk-organizer
npm ci --ignore-scripts
npm run check
```

## 分支与提交

1. 从默认分支创建短生命周期分支。
2. 每次提交只处理一个清晰问题。
3. 不提交真实账号数据、网盘清单、运行状态或构建缓存。
4. 在 PR 中说明目的、影响范围、测试证据、风险和回滚方式。

## 隐私规则

测试和文档只能使用虚构占位符。禁止提交：

- OAuth 成功网址、令牌、Cookie、验证码或密码
- `*.dpapi`、`.env`、OAuth 临时状态或令牌元数据
- 真实账号、文件名、目录、媒体、下载链接或整理报告
- 本机用户名、绝对安装路径、终端历史或运行日志
- 可执行的当前写操作确认码

即使凭据已过期，也不能作为测试夹具提交。测试令牌必须明显标注为虚构值。

## 安全不变量

涉及写操作的 PR 必须证明：

- 默认配置仍是只读
- 写入和删除仍需分别启用
- 安全根不能是 `/`
- 所有 mutation 都经过 `prepare -> 当前消息确认 -> execute`
- 确认码绑定完整计划、具有有效期且只能使用一次
- 执行前重新核对远端状态
- 同名冲突失败，不覆盖
- 执行后回读验收
- 未知结果不自动重试
- 远端写入前持久化隔离，只有完整验收成功才自动解除
- 目录后代身份摘要在执行前重新核对，读取上限必须失败关闭
- 不新增清空回收站、上传、复制或分享能力

任何绕过上述约束的修改都不应合并。

## 测试要求

- 新功能和缺陷修复应包含最小自动化测试。
- 网络测试必须使用本地模拟服务和虚构令牌。
- 测试不得访问真实百度账号或真实网盘目录。
- 端点允许列表、秘密脱敏、计划过期、单次使用、状态变化、冲突拒绝和执行后回读都应有覆盖。
- 仅有“接口返回成功”不算验收；写操作测试必须验证预期状态。

## 文档要求

- 默认 README 使用中文，`README.en.md` 保持英文镜像。
- 命令使用仓库相对路径或清晰占位符。
- 新增工具、脚本、门禁或恢复边界时，同时更新 README 和 SECURITY。
- 不在示例中使用真实用户名、设备名、网盘内容或操作记录。

## PR 检查清单

- [ ] 变更范围最小且可回滚
- [ ] `npm run check` 通过
- [ ] 没有真实凭据或个人数据
- [ ] 没有降低默认安全门禁
- [ ] 中英文 README 已同步
- [ ] 风险、限制和恢复方式已写清
- [ ] 第三方依赖变化已更新通知文件

安全问题不要走普通 Issue；请按 [SECURITY.md](SECURITY.md) 私密报告。
