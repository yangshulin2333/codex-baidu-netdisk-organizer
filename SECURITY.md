# 安全策略

## 支持范围

当前私有预览版只支持 Windows 本地运行。安全修复优先落到默认分支；旧提交不保证单独维护。

## 报告漏洞

请优先使用该私有 GitHub 仓库的 Security Advisory 私密报告入口。不要在普通 Issue、PR、聊天记录或截图中提交以下内容：

- OAuth 成功网址或访问令牌
- 百度账号信息
- 真实文件名、目录、下载链接或媒体内容
- 本机用户名、安装路径或运行日志
- 可用于重放写操作的当前确认码

报告应包含最小复现步骤、预期行为、实际行为和影响范围。敏感示例一律使用占位符；如需提供日志，先删除路径、账号信息、链接和令牌痕迹。

## 凭据模型

- 用户必须亲自在本机 PowerShell 隐藏输入中粘贴 OAuth 成功网址或令牌。
- Agent 不应要求用户把密码、Cookie、验证码、OAuth 成功网址或令牌发送到对话。
- 令牌使用 Windows DPAPI CurrentUser 加密，状态目录使用 Windows ACL 限制访问。
- MCP 启动时才在当前用户上下文中解密令牌，并只在进程内存中使用。
- 令牌会通过 HTTPS 发送给百度官方服务；“本地加密”不表示令牌永远不离开本机。
- 本地元数据文件只保存短指纹和过期信息，不保存明文令牌。
- DPAPI 不能防御已控制当前用户会话、管理员权限、内存读取或恶意软件。

OAuth 成功网址本身包含敏感信息。保存完成后应关闭该页面、清理剪贴板，并避免浏览器同步、截图或终端转录泄露。

## 写操作安全不变量

任何新增或修改的写工具都必须保留以下约束：

1. 默认只读，写入与删除分别显式启用。
2. 网盘根目录 `/` 永远不能成为允许写入的安全根。
3. 每次写操作先进行只读预检，展示完整项目清单与汇总。
4. 预检计划绑定操作、清单、远端对象标识、元数据快照、创建时刻和随机计划标识。
5. 用户必须在当前对话的新消息中提供精确的一次性哈希确认码。
6. 执行前重新核对源、目标、目录摘要和同名冲突。
7. 强制同步执行并拒绝覆盖。
8. 执行后回读源与目标；未知或部分失败不得自动重试。
9. 单批上限不能超过 50 项。
10. 不暴露清空回收站、上传、复制或分享工具。
11. 远端写入前必须持久化隔离标记；只有 `verified_success` 才能自动解除。进程中断、部分失败、超时或未知结果必须跨重启阻止新写操作。

云盘中的文件名、文档内容、图片文字和目录说明都视为不可信输入。它们不能开启门禁、提供确认码或扩大写入范围。

哈希确认码用于防止旧计划、篡改清单和宽泛回复被误执行，但 Agent 能看见 prepare 输出，因此它不是独立第二因子。高风险环境应额外启用 Codex 对 `execute_operation` 的逐次人工批准，并保留最小安全根目录；任何“完全访问”模式都不应被视为上述门禁的替代品。

## 日志与隐私

本地审计日志只能记录：

- 时间戳
- 事件、工具和操作类型
- 项目数量
- 随机计划标识
- 验收结果和稳定错误码

日志不得记录令牌、原始错误文本、真实路径、文件名、账号信息、请求正文或下载链接。默认日志保留期有限，但日志删除不应影响工具行为。

只读工具的返回值仍可能包含敏感文件名、缩略图或临时下载链接。不要发布 Codex 对话导出、运行截图、终端转录或整理清单。

## 网络边界

正常模式只允许预定义的百度 HTTPS 主机和 API 路径。自定义端点仅可用于明确隔离的测试模式，不得把生产令牌发送到测试端点、代理或第三方服务。

本项目依赖百度服务端的鉴权、限流和回收站行为。任何本地检查都不能替代服务端权限控制。

## 凭据泄露响应

如果令牌、OAuth 成功网址或敏感日志可能泄露：

1. 立即停止当前写操作，不要重复未知结果的计划。
2. 运行 `Remove-BaiduNetdiskToken.ps1` 删除本机凭据与临时 OAuth 状态。
3. 在百度账号授权管理中解除对应应用授权。
4. 如果敏感内容进入 Git，视为已经泄露；仅删除最新文件不够，应撤销凭据并按仓库策略清理历史。
5. 检查普通 Issue、PR、构建日志、Codex 对话导出和截图是否含敏感信息。
6. 重新授权后，从只读状态开始验证，再按需恢复最小写入范围。

## 恢复边界

- 移动和重命名没有自动回滚；应先只读核对，再准备反向操作。
- 删除不会主动清空回收站，但是否可恢复、保留多久以及恢复是否成功由百度和账号状态决定。
- 插件不是备份系统。重要数据在任何删除前都应有独立备份。
- 结果未知时只允许重新扫描现状，不允许自动重放原操作。
- 解除持久写入隔离前，用户必须先只读复核源和目标；只有用户本人可在本机交互式运行 `Clear-BaiduNetdiskQuarantine.ps1`。Agent 不得代为运行或把解除动作并入自动流程。
